CREATE TABLE sales (
id int, 
invoice int, 
make VARCHAR(150), 
model VARCHAR(150), 
price int);

create table cust_random (
id_num int,
make VARCHAR(150), 
model VARCHAR(150), 
price int);

create table service(
customer_id int,
service_number int,
serial_number VARCHAR(150),
mechanic VARCHAR(150)
);




